# Example .env - copy to .env when developing locally
# For production on Vercel, set Environment Variable DATABASE_URL in Project Settings

# Example Postgres (replace with your provider's connection string)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
